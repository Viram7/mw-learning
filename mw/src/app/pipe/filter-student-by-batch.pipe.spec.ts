import { FilterStudentByBatchPipe } from './filter-student-by-batch.pipe';

describe('FilterStudentByBatchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStudentByBatchPipe();
    expect(pipe).toBeTruthy();
  });
});
